services:
  - type: web
    name: digitalriskplatform
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 30 --workers 1 --threads 2
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
